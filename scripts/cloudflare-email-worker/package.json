{
  "name": "gmailm-inbound-email-worker",
  "private": true,
  "type": "module",
  "description": "Cloudflare Email Worker：Email Routing → gmailM inbound webhook",
  "scripts": {
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put WEBHOOK_SECRET",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "postal-mime": "^2.4.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
